# Leave empty to use same-origin API paths. During local development, Vite
# proxies /api requests to http://localhost:8000.
VITE_API_URL=

# Development server settings. Keep the loopback default unless remote access
# is explicitly required.
DEV_SERVER_HOST=127.0.0.1
DEV_SERVER_ALLOWED_HOSTS=
